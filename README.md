A OSC API for Resolume Arena 7 that allows you to trigger a column with a fade time in seconds.

**Usage
Run the binary (Linux AMD64 and Windows AMD64 builds provided) - the config.json must be in the same directory as the executable

Send OSC requests to the port in the format /column {Column ID} {Transition time in seconds}
